# BRANCHES — every branch not master lives here until merged+deleted

Status is the four-state vocabulary — DONE / BLOCKED / OWED / UNSTARTED — per the shared
loop-rules block in `CLAUDE.md`. No fifth state. BLOCKED must name a blocker that holds
*now* and an owner; where the evidence does not settle barrier-vs-trigger, the row is
UNSTARTED.

| Branch | Purpose | Status | Detail | Blocker / outstanding (owner) |
|--------|---------|--------|--------|-------------------------------|
| ci/placeholder-guard-hca | `#24` — `#170`'s CI placeholder guard propagated from health-app (`.github/workflows/governance-guard.yml`) | OWED | Executable body `diff`-empty against health-app `d9f45f3`; header adapted at four sites where health-app's asserts evidence about its own repo — notably its ruleset, which HCA does not have. Four real Actions runs: PR #11 green (negative), PR #12 red at 2c, PR #13 red at 2a, PR #14 red at 2b (exit 126). All three control PRs closed, branches deleted local+remote. | Fast-forward to master, resolved to **#24** against `origin/master`'s max (`### #23`) re-read at that instant. Then the ruleset — GitHub-side, not committable, owner Luke (`Q12`). |
| gov/placeholder-guard-hca | Placeholder guard (`.githooks/pre-push` + `scripts/check_governance_placeholders.py`) and the post-Part-A shared block propagated from health-app `44a4d28` | DONE → `78f460e` | Guard files `diff`-empty against source; shared block `diff`-empty post-copy, 155 -> 215 lines; `core.hooksPath .githooks` installed in this clone; hook proven to fire by a synthetic both-arms refusal on a throwaway ref (`2a3426d`, deleted), plus five passing negative controls. Carries three unresolved placeholders by design — one `#NEXT`, two `Q#NEXT` — which the guard itself flags. | Merged+deleted local and remote; `git cherry origin/master` empty. Tokens were resolved on-branch pre-ff against `origin/master`'s maxima re-read at that instant (`### #21` / `### Q11`): claimed **#22**, **Q12**, **Q13**. Follow-on `gov/hook-exec-bit` (`b7e3bf5`, **#23**) corrected the hook's tracked mode to `100755` and is likewise merged+deleted. Nothing outstanding. |
| fix/hrv-capture-regression | HRV auth-path guard-proof test (fix itself already on master by patch) | UNSTARTED | Pushed; local == origin at `7888067`. `git cherry` 4 `+`, 1 `-`. The row's own text names a D2 firewall-gap dependency but does not settle whether that dependency is a barrier or a trigger — the tie-break resolves it to UNSTARTED rather than assert work is impossible. | Context, not a blocker: relates to the #8 D2 HRV-capture firewall-gap work (Brief 1). |
| feat/hrv-node-dump | Read-only node-tree dump instrumentation (`dumpTree`/`dumpActiveTree` in `HRVAccessibilityService.kt`) | BLOCKED | +1 vs origin/master (`b66d34b`, `git cherry` `+`); rebased onto master so it carries the #19 fix + Metro guard. Pushed to `origin/feat/hrv-node-dump` 2026-07-20 — no longer local-disk-only. Earned its keep 2026-07-13: the dump it wrote (`nodedump.txt`) proved the scraper healthy and redirected a "broken scraper" brief to the real cause (SyncScreen `deepIfConst4` crash, fixed `e677f9e`) — strengthens keep-behind-a-flag over strip. The app-process crash blocker cleared; the sync blocker did not. | One overnight/~5am sync on the standalone build landing today's HRV in Railway — owner Luke. |
| fix/scraper-sh-relayout | SH scraper phantom-node fix | DONE → `1db8833` | Landed on master (DECISIONS_LOG #19, renumbered from stale #16); `.kt` byte-identical upstream. Residual `git cherry` `+` was stale #16 log wording (superseded by #19) plus 2 chore close-outs, dropped — code fully landed. Force-deleted. | — |
| chore/block-metro-debug-build | Metro-block guard (hook + npm script + FEEDBACK) | DONE → `db6f50e` | Landed on master via clean `--ff-only`; `git diff master..chore` empty and `git cherry` clean (byte-identical upstream). Safe-deleted (`git branch -d`, true ancestor). | — |
| claude/hevy-api-workout-query-teulc2 | Session close-out from 2026-07-07 (`BRANCHES.md` / `ROADMAP.md` / `closeout.md` only) | DONE → discarded 2026-07-20 | **Deleted from origin by the operator, 2026-07-20.** Verified from this repo: `git ls-remote --heads origin claude/hevy-api-workout-query-teulc2` returns empty, and the remote-tracking ref pruned. **Discarded, not merged** — its single commit `4dfccbe` was `git cherry` `+` (unlanded by patch-id) to the end, and nothing was salvaged: the content was a 2026-07-07 close-out superseded by every close-out since. Carried no row here until 2026-07-20 — the omission health-app Q25 records; adding the row resolved Q25, and this deletion closes the disposition. The `claude/<hash>` auto-name violated CLAUDE.md's branch-naming rule, which was the supporting rationale. | — (complete). |
