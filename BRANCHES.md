# BRANCHES — every branch not master lives here until merged+deleted

Status is the four-state vocabulary — DONE / BLOCKED / OWED / UNSTARTED — per the shared
loop-rules block in `CLAUDE.md`. No fifth state. BLOCKED must name a blocker that holds
*now* and an owner; where the evidence does not settle barrier-vs-trigger, the row is
UNSTARTED.

**DONE-row retention (`Q6`, resolved #NEXT).** A live branch is always rowed. A
merged+deleted branch's row is **required** only when a store cites an artefact produced on
that branch — a CI run ID, control output, or a SHA quoted as evidence — so the citation
resolves to an accounted-for branch, not a dangling ref. Otherwise merged+deleted satisfies
"until merged+deleted" above and **no row is required** (a retained row is permitted, not
mandated). This is why the four control branches are rowed — their run IDs are cited in
`#24`/`#28` — and `gov/close-q12` is not: `#28` quotes its refusal output inline, pointing at
nothing that needs a row.

| Branch | Purpose | Status | Detail | Blocker / outstanding (owner) |
|--------|---------|--------|--------|-------------------------------|
| scratch/gov-control-placeholder | `#24` positive control — synthetic `### #NEXT` + `### Q#NEXT` in HCA's own grammars | DONE → discarded 2026-08-07 | PR #12, run `31172614745`: **red at 2c** against the merge commit, naming both offences. Closed unmerged; branch deleted local and remote. **Discarded, not merged** — the content was never for master. |
| scratch/gov-control-mode | `#24` positive control — `.githooks/pre-push` tracked `100644` | DONE → discarded 2026-08-07 | PR #13, run `31172624557`: **red at 2a**, `tracked mode: 100644`. `#23` reproduced deliberately on a POSIX surface, the first time that class was observable at all. Closed unmerged; branch deleted local and remote. |
| scratch/gov-control-exec | `#24` positive control — 2a stripped so it could not short-circuit, hook still `100644` | DONE → discarded 2026-08-07 | PR #14, run `31172640907`: **red at 2b**, `./.githooks/pre-push: Permission denied`, exit 126. Minted because the mode control never ran 2b — without it the execution arm was argued, not shown. Closed unmerged; branch deleted local and remote. |
| control/ruleset-merge-gate | Operator's merge-gate proof for ruleset `20573455` | DONE → `5459886` | PR #15, merged by the web-UI button — a server-side ref update, committed by `GitHub <noreply@github.com>`, i.e. the very path `Q12` was opened about, now passing through the gate rather than around it. Branch deleted. |
| gov/block-remirror | Shared-block re-mirror (G1 breach, health-app ahead), HCA-local `land`, four stale-row repairs | DONE → `3bd196d` | Block 215/15132/592d95c8 -> 259/18717/552728ad, `cmp` silent. Four HCA-only lines deleted, all deliberately replaced upstream; diff 48/4, proportional. `land` set `--local`, verified with the discriminating form (#103); health-app local alias re-verified intact. Q8 and Q11 closed against health-app master, Q12 amended, Q14 minted as the mirror of health-app Q33. | Merged+deleted; never pushed to origin. Outstanding: the ruleset only — GitHub-side, not committable, owner Luke (Q12, OWED). |
| ci/placeholder-guard-hca | `#24` — `#170`'s CI placeholder guard propagated from health-app (`.github/workflows/governance-guard.yml`) | DONE → `6483d19` | Executable body `diff`-empty against health-app `d9f45f3`; header adapted at four sites where health-app's asserts evidence about its own repo — notably its ruleset, which HCA does not have. Four real Actions runs: PR #11 green (negative), PR #12 red at 2c, PR #13 red at 2a, PR #14 red at 2b (exit 126). All three control PRs closed, branches deleted local+remote. | Merged+deleted (PR #11 shows MERGED; remote ref auto-deleted). Both workflow arms since exercised on master: `push` runs `31172871257` and `31173021814`, both green. Follow-on `gov/decisions-log-crlf-repair` (`e3324ba`, **#25**) likewise merged+deleted. **Outstanding: the ruleset only** — GitHub-side, not committable, owner Luke (`Q12`, OWED). |
| gov/placeholder-guard-hca | Placeholder guard (`.githooks/pre-push` + `scripts/check_governance_placeholders.py`) and the post-Part-A shared block propagated from health-app `44a4d28` | DONE → `78f460e` | Guard files `diff`-empty against source; shared block `diff`-empty post-copy, 155 -> 215 lines; `core.hooksPath .githooks` installed in this clone; hook proven to fire by a synthetic both-arms refusal on a throwaway ref (`2a3426d`, deleted), plus five passing negative controls. Carries three unresolved placeholders by design — one `#NEXT`, two `Q#NEXT` — which the guard itself flags. | Merged+deleted local and remote; `git cherry origin/master` empty. Tokens were resolved on-branch pre-ff against `origin/master`'s maxima re-read at that instant (`### #21` / `### Q11`): claimed **#22**, **Q12**, **Q13**. Follow-on `gov/hook-exec-bit` (`b7e3bf5`, **#23**) corrected the hook's tracked mode to `100755` and is likewise merged+deleted. Nothing outstanding. |
| fix/hrv-capture-regression | HRV auth-path guard-proof test (fix itself already on master by patch) | UNSTARTED | Pushed; local == origin at `7888067`. `git cherry` 4 `+`, 1 `-`. The row's own text names a D2 firewall-gap dependency but does not settle whether that dependency is a barrier or a trigger — the tie-break resolves it to UNSTARTED rather than assert work is impossible. | Context, not a blocker: relates to the #8 D2 HRV-capture firewall-gap work (Brief 1). |
| feat/hrv-node-dump | Read-only node-tree dump instrumentation (`dumpTree`/`dumpActiveTree` in `HRVAccessibilityService.kt`) | UNSTARTED | +1 vs origin/master (`b66d34b`, `git cherry` `+`); rebased onto master so it carries the #19 fix + Metro guard. Pushed to `origin/feat/hrv-node-dump` 2026-07-20 — no longer local-disk-only. Earned its keep 2026-07-13: the dump it wrote (`nodedump.txt`) proved the scraper healthy and redirected a "broken scraper" brief to the real cause (SyncScreen `deepIfConst4` crash, fixed `e677f9e`) — strengthens keep-behind-a-flag over strip. The app-process crash blocker cleared 2026-07-13; the sync blocker cleared 2026-08-08 on `Q4`'s close (`DONE → operator observation`). `nodedump.txt` itself remains untracked; its commit/gitignore/delete disposition is open — owner Luke. | None (blocker cleared by `Q4`). Non-blocking: keep-behind-a-flag vs strip disposition remains open — owner Luke. |
| fix/scraper-sh-relayout | SH scraper phantom-node fix | DONE → `1db8833` | Landed on master (DECISIONS_LOG #19, renumbered from stale #16); `.kt` byte-identical upstream. Residual `git cherry` `+` was stale #16 log wording (superseded by #19) plus 2 chore close-outs, dropped — code fully landed. Force-deleted. | — |
| chore/block-metro-debug-build | Metro-block guard (hook + npm script + FEEDBACK) | DONE → `db6f50e` | Landed on master via clean `--ff-only`; `git diff master..chore` empty and `git cherry` clean (byte-identical upstream). Safe-deleted (`git branch -d`, true ancestor). | — |
| claude/hevy-api-workout-query-teulc2 | Session close-out from 2026-07-07 (`BRANCHES.md` / `ROADMAP.md` / `closeout.md` only) | DONE → discarded 2026-07-20 | **Deleted from origin by the operator, 2026-07-20.** Verified from this repo: `git ls-remote --heads origin claude/hevy-api-workout-query-teulc2` returns empty, and the remote-tracking ref pruned. **Discarded, not merged** — its single commit `4dfccbe` was `git cherry` `+` (unlanded by patch-id) to the end, and nothing was salvaged: the content was a 2026-07-07 close-out superseded by every close-out since. Carried no row here until 2026-07-20 — the omission health-app Q25 records; adding the row resolved Q25, and this deletion closes the disposition. The `claude/<hash>` auto-name violated CLAUDE.md's branch-naming rule, which was the supporting rationale. | — (complete). |
